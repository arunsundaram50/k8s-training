apiVersion: batch/v1
kind: CronJob
metadata:
  name: hello-world-cronjob
spec:
  schedule: "*/1 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: hello-world
            image: busybox
            args:
            - /bin/sh
            - -c
            - "echo 'Hello World $(date)' >> /data/greetings.txt"
            volumeMounts:
            - name: data-volume
              mountPath: /data
          volumes:
          - name: data-volume
            hostPath:
              path: /home/asundaram/apps/docker-training/kd12-cronjob-minikube-volume-mount/data
              type: DirectoryOrCreate
          restartPolicy: OnFailure
